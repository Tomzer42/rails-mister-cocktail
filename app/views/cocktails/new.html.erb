<div class="container">
  <h1 class="d-flex justify-content-center border m-2">Create a new cocktail</h1>
  <div class="form_create">
    <%= simple_form_for(@cocktail) do |f| %>
      <%= f.input :name %>
      <%= f.input :photo, as: :file %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
